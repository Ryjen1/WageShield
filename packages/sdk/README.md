# @wageshield/sdk

Client SDK for WageShield. Wraps `@cofhe/sdk` + `ethers` v6 with project-specific
helpers for building issuer attestations, submitting claims, and decrypting via
CoFHE permits.

> **Status:** placeholder — implementation lands in Wave 5. The reference
> implementation today lives in
> [`packages/contracts/scripts/e2e-live.ts`](../contracts/scripts/e2e-live.ts).
